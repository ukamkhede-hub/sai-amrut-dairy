name: Sai Amrut Dairy CI

on:
  push:
    branches:
      - main

jobs:
  docker-build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Build Docker Image
        run: docker build -t sai-amrut-dairy .

      - name: Test Docker Image
        run: docker images sai-amrut-dairy
        